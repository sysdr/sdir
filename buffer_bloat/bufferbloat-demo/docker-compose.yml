version: '3.8'

services:
  demo:
    build: .
    container_name: bufferbloat-demo
    privileged: true
    volumes:
      - ./app:/app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
